test:
  override:
    - "ssh-add ~/.ssh/id_rsa"
    - "ssh-add -l"
    - "chmod +x bootstrap.sh"
    - "sudo ./bootstrap.sh"
    - "sudo make test"
    - "sudo make full_clean"
    - "sudo make install"
    - "sudo make test"
    - "make count"
